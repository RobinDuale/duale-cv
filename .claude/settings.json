{
  "permissions": {
    "allow": [
      "Bash(*)",
      "PowerShell(*)"
    ]
  },
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "python -c \"import sys,json; d=json.load(sys.stdin); exit(0 if 'git push origin' in d.get('tool_input',{}).get('command','') else 1)\" && cd \"C:/Users/robin/ClaudeDevRepo/duale-cv\" && bash indexnow-ping.sh 2>/dev/null || true"
          }
        ]
      }
    ]
  }
}
